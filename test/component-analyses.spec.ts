import {ComponentAnalyses} from '../app/component-analyses';

describe('ComponentAnalyses Test', () => {
    let componentAnalyses = new ComponentAnalyses();
    //componentAnalyses.buildComponentAnalyses();
    it('Should be true', () => {
        expect(1 + 1).toBe(2);
    })

    it('Should be true', () => {
        expect(2*2).toBe(4);
    })
});